[package]
name = "macros"
version = "0.1.0"
edition = "2024"

[dependencies]
tracing = "0.1.41"
tracing-log = "0.2.0"
tracing-subscriber = "0.3.19"
serde = { version = "1.0.219", features = ["derive"] }
enigo = { version = "0.5.0", features = ["serde", "x11rb"] }
slotmap = { version = "1.0.7", features = ["serde"] }
tokio = { version = "1.47.1", features = ["rt", "rt-multi-thread", "macros"] }

[dependencies.libcosmic]
git = "https://github.com/pop-os/libcosmic.git"
rev = "8412dd5"
version = "0.1.0"
default-features = false
features = [
    "debug",
    "winit",
    "tokio",
    "xdg-portal",
    "dbus-config",
    "a11y",
    "wayland",
    "wgpu",
    "single-instance",
    "multi-window",
    "serde-keycode"
]

[build-dependencies]
